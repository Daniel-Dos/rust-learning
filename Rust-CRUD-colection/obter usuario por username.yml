info:
  name: obter usuario por username
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/api/users/{{username}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
