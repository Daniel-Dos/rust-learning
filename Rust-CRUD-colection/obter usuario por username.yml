info:
  name: obter usuario por username
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/api/users/{{user_id}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
