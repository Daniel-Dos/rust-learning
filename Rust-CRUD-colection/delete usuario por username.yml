info:
  name: delete usuario por username
  type: http
  seq: 4

http:
  method: DELETE
  url: http://localhost:8080/api/users/{{username}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
