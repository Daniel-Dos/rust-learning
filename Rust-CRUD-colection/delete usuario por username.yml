info:
  name: delete usuario por username
  type: http
  seq: 5

http:
  method: DELETE
  url: http://localhost:8080/api/users/{{user_id}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
