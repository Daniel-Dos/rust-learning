info:
  name: cria um novo usuario
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/api/users
  body:
    type: json
    data: |-
      {
       "username":"Teste",
        "email": "teste@teste.com",
        "age": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
